# required by a command running non-interactively
# should be updated on each new shell
# (read every time)
#
# usage example:
#
# - environment variables which need to be updated frequently (such as `PATH`)
